mixin video(info)
    div 
        a(href=)
        ul  
            li #{info.rating}/5
            li #{info.comments} comments.
            li Posted #{info.createdAt}
            li #{info.views} view